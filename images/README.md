# Supplied project images

A.png through D.png are sample traffic images included in the original project files. refrence.png is the supplied reference image; its original filename is retained for compatibility.

Run `python test_script.py` from the repository root to process these samples.
